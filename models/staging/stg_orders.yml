version: 2
models:
  - name: stg_orders
    description: Internal cleaned order staging model.
    columns:
      - name: order_id
        data_tests: [not_null, unique]
      - name: customer_id
        data_tests: [not_null]
      - name: order_total
        data_tests: [not_null]

version: 2
models:
  - name: stg_customers
    description: Internal cleaned customer staging model.
    columns:
      - name: customer_id
        data_tests: [not_null, unique]
      - name: customer_name
        data_tests: [not_null]

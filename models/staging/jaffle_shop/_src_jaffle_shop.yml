sources:
  - name: jaffle_shop
    database: valentin-pfeiffer-masterarbeit
    schema: dbt_architect_stg
    description: A clone of a Postgres application database.
    config: 
      freshness:
        warn_after:
          count: 12
          period: hour
        error_after:
          count: 100
          period: day
      loaded_at_field: _etl_loaded_at
    tables:
      - name: customers
        description: Raw customer data.
        columns:
        - name: id
          description: Primary key for the customer data.
          data_tests:
            - unique
            - not_null
        config:
          freshness: null
      - name: orders
        description: Raw orders data.
        columns:
        - name: id
          description: Primary key for the orders data.
          data_tests:
            - unique
            - not_null